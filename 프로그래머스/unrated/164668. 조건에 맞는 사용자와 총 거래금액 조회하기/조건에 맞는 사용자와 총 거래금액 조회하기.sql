-- 코드를 입력하세요
SELECT USER_ID, NICKNAME, B.TOTAL_SALES FROM USED_GOODS_USER AS U INNER JOIN (SELECT WRITER_ID, SUM(PRICE) AS TOTAL_SALES FROM USED_GOODS_BOARD WHERE STATUS = 'DONE' GROUP BY WRITER_ID HAVING TOTAL_SALES >= 700000) AS B ON U.USER_ID = B.WRITER_ID ORDER BY TOTAL_SALES;